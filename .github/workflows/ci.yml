name: Test

on: [push]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Install dependencies
        run: sudo apt-get install libxkbcommon-x11-dev libwayland-dev
      - name: Build
        run: cargo build
      - name: Test
        run: cargo test


